package com.dyno.tv_notification_mirror.phone

import io.flutter.embedding.android.FlutterActivity

class MainActivity: FlutterActivity() {
    // The native methods and events are now handled by the local plugin `native_bridge`
    // This allows them to be registered on both UI isolate and background isolates.
}
